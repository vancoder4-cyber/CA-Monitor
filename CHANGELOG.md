# 更新日志

> 规则:每次修复、规则/配置/数据修订或用户可见变更完成时,立即在最上面加一条,不得等到 push 或部署再补。格式 `## 日期 · 标题` + 几条 `- 要点`。日期用**真实当天日期**(可 `date +%F` 确认),别照抄上一条。只读查询且仓库无变化时可免。保持简洁。

## 2026-09-05 · 推送负责人按现货与合约分流

- 定时 Lark 的正式催办、合约需操作结论与超期异常改为按产品归属选择负责人：现货组与合约组各用独立私密名单，同时覆盖现货+合约的标的会合并两组并去重。
- 保持原有提醒边界：单源预测、公司行动条款/合约门槛核验和仅合约且无需操作事项不会新增正式 @；现货+合约标的若现货侧仍需执行，则按双产品规则通知两组并明确展示合约无需操作。
- 工作流、环境变量模板、操作手册、群内说明和发布检查表已同步；旧全局名单仅作迁移期兜底，人员 open_id 继续只存在于 GitHub Actions Secrets，不进入仓库、网页或日志。

## 2026-09-04 · 恢复问答助手写操作权限

- 补齐 Railway 的写操作员白名单配置，恢复已授权操作员的确认、观察、备案结论和需求提报；只读查询不受影响。
- 经身份核验新增一名写操作员，保留原有白名单成员；新增授权仍仅作用于上述写操作。
- 白名单值仅保存在 Railway Secret，不写入仓库、Pages 数据或卡片；新部署已恢复 Lark 长连接。

## 2026-09-04 · 发布全量操作手册

- 新增 `OPERATIONS_MANUAL.md`，把网站、定时 Lark、CA 问答助手全部 16 个指令、现货/合约动作、3% 门槛、三类核验、人工确认与备案结案整理成一份统一运营 SOP。
- 补齐每日值班、外部老虎公司行动 API 字段映射、GitHub/Pages/Railway 发布、Secrets、故障处理、安全红线和一页式验收清单；同步重写群内 briefing 与人工确认清单，根 README、Bot 部署文档及更新检查清单均加入入口和同步门禁。
- 文档按实际 workflow 将运行日明确为「美东周一至周五，尚未接交易所节假日日历」，并补充 Lark 已发送但后续快照校验/cache 失败时的重跑防重复流程，不再过度承诺端到端 exactly-once。
- 指令一致性门禁从 README/Bot 文档扩展到全量操作手册：CI 精确比对手册指令表的 16 个名称，并继续检查其余文档覆盖；说明文字与关键词仍按人工清单复核。
- 修正文档验收边界：生产 provenance 完整字段从 `data.json` 核对，网站红条是警告而非隐藏旧内容；本地 build 强制关闭 Lark 并隔离 state，失败重跑前必须确认群内是否已实际收到卡片。
- 明确当前确认/观察身份及备注仍写入公开 Git 的遗留隐私边界，并修正风险卡不会收录「仅合约且已明确 ≤3% 无需操作」事项的实际查询路径。
- 补严人工确认门禁：现金/送股只接受有限正数，拆合股的新确认必须使用完整 `新股数:旧股数`；空值、0、负数、无效格式和备注中的 `8-K`/年份不再被误读，也不能消除冲突、空缺或污染公开快照，存量合法单因子比例仍兼容读取。

## 2026-09-04 · 收口公司行动跨展示面一致性

- 分红的主事件日继续显示「除息」，拆股/合股在网站、文本 digest、定时 Lark、问答助手的新公告/临近催办/查代码中统一显示「生效」；旧快照里写死的错误催办文案也会在展示边界纠正，T0 不再误写成「仅剩 1 天」。
- 网站 SEC 区更名为「普通备案 + 公司行动相关文件」，明确普通备案仅供原文审计；网页的 pending、预测、冲突、空缺、人工确认和统计均增加与 Bot/Lark 相同的二次过滤。
- SEC 补收 8-K/A、6-K 与 6-K/A：8-K/A 沿用严格 Item 门禁；外国发行人报告默认不触发公司行动，只有文件名/描述命中强提示时进入「疑似相关·待核实」。强短语匹配会排除 Distribution Agreement、合并财务结果及债券赎回等普通文件，避免 ARM/BABA/TSM 漏掉审计入口或被全量误报。
- 「单源核验 / 公司行动条款核验 / 合约 3% 门槛核验」使用独立状态与文案；疑似 6-K 即使同时覆盖现货也不得升级为执行催办或正式 @，后续确认时又不会被先前核验记录吞掉。
- filing 条款状态按稳定事件 ID 持久化：事件日已过仍未解决时每个业务日继续核验；跨日转为正式公司行动或普通备案时只推一次明确结论，正式执行项会 @，普通备案明确「本次无需操作」且不 @，网站、digest 与问答助手同步展示。
- 问答助手新增 `确认备案` / `排除备案` 结案指令，公开 Git 只保存匿名业务结论；仅分红提示且申报日精确匹配正式分红宣告日的 6-K 会自动并入证据链并关闭重复核验。未人工处理的疑似 filing 在 30 天后转为未决归档并停止日报，但不会被误写成「无需操作」。
- Pages → Railway 公开数据契约升级为 schema v4，锁定上述三种核验语义；新旧 producer / Bot 在错开发布时会 fail closed，不会把待核实 6-K 误算为正式事项。
- 合入 `main` 或 Bot 写入实际生效状态后会自动触发生产监控工作流，及时刷新 Pages、`data.json`、Lark 与 Bot「最近更新」数据源；只追加审计日志或需求队列时不重复跑生产，不再依赖下一定时窗口或人工触发。
- 周末合入或 Bot 状态写回生成的快照会把有效期延续到下一个周一计划扫描窗口；备案结案严格校验完整事件 ID，已退出当前抓取窗口的备案在确认后也会重新生成正确的现货/合约动作，不再沿用旧的「条款待核实」。
- 问答助手的确认、观察、备案结论和需求提报统一增加 Railway 操作员白名单门禁；鉴权发生在取快照与 GitHub 写回之前，未配置、发送人缺失或未授权一律拒绝，查询和审计不受影响。
- 新增跨展示面、旧快照防御、SEC 外国发行人表格、核验类型与精确日期词回归；维护文档同步加入检查项和保守能力边界。

## 2026-09-03 · 修复普通 SEC 8-K 被误报为公司行动

- SEC 8-K 只有破产/接管、完成收购或资产处置、退市、证券持有人权利变更、控制权变更等可仅凭 Item 号确定的结构性事项才进入公司行动流。
- 1.01 / 2.02 / 5.02 / 5.07 / 7.01 / 8.01 等宽泛披露仍保留在网站 SEC 原文表，但不再进入日历、风险、今日/本周、临近、查代码、定时推送或人工异常计数，避免财报/融资/高管变动触发「下架或暂停交易」假风控。
- Pages 生成器、定时 Lark、问答卡片和 Bot PNG 月历均增加边界过滤；历史或异常快照含普通 filing 时也不会重新泄漏到公司行动展示面。
- 同步修正本地验收文档：清洁 checkout 不再无条件校验已迁出 Git 的 `data/state.json`；生产恢复与 fail-closed 校验仍由 Action 强制执行。

## 2026-09-03 · 完成网站、定时推送与问答助手联动检查

- Pages 数据升级为可验证的 schema v3，统一下发生成时间、有效时点、源码 commit、Action run 与投递状态；网站和问答助手遇到旧版、缺字段、未来时间或过期快照时会显式停用业务结论，不再把故障误报成「无风险」。
- 修复问答指令被备注中的「公告/更新」劫持、确认/观察部分写入失败却报成功、拆合股比例被截断、结构性行动首报后从查询消失，以及现货+合约低影响分红在风险卡被漏掉的问题；15 个指令均新增端到端回归。
- 网站、定时 Lark、Bot 卡片和月历统一按稳定事件 ID、例行备案分类、数值门禁与产品动作结论展示；同一事项按「催办 → 状态更新 → 公告 → 新发现」只显示最高优先级一次，`≤3%` 合约事项仍正常报告但不会误触发执行催办或 @。
- 「最近更新」不再截掉同一版本的第 7 条以后内容；Markdown 嵌套列表、粗体、代码和安全链接可在网页完整显示，构建与 Bot 共用同一个解析结果。
- GitHub Actions 将去重状态与辅助缓存分离，恢复失败即停止历史重放；Lark 投递失败不再覆盖 Pages，公开快照发布前会校验数据契约与脱敏，PR CI 同时跑全量测试、跨展示面检查和 Bot 容器构建。
- 发布验收会锁定本次 `workflow_dispatch + main commit + run id`，并核对 Pages 的 schema、源码版本、投递状态和更新日志；催办 @ 名单改用 GitHub Secret，需求提报以匿名编号写入公开仓库且明确禁止填写敏感信息。

## 2026-09-03 · 明确合约公司行动 3% 操作门槛

- 公司行动继续正常采集、展示和首报，但与合约执行提醒分离：现金分红、送股、拆股、合股等的估算价格影响严格 **>3%** 才标记「合约：需操作」；3% 或以下固定显示「合约：本次无需操作」。
- 金额、比例、参考价日期、币种/证券单位缺失或未过数据门禁时只做「合约：待核实」；ADR/ADS 口径不一致、普通非公司行动 8-K 均会安全分流，不会武断下结论。
- 仅合约且无需操作的事项不再进入 30/14 日重复催办或触发正式 @；现货+合约仍保留现货流程。网页、定时 Lark、Bot、日历和 digest 统一消费同一产品动作结论。
- 抓取阶段新增前一完整交易日的未调整收盘价快照（Tiingo 优先、yfinance 回退、跨运行保留 last-known-good）；修复小比例拆合股被整数化及精确 3% 比较，并在结论变化时主动推送一次更新。
- 公司行动是否正式与金额/比例是否可执行分开判断：正式事件数值冲突时仍正常报告，但合约结论保持待核实；历史催办迁移为无需操作时会主动发一次解除结论。
- 人工确认按「代码+事件类型+日期」生效并完整保留拆/合股 `新股数:旧股数`；送股、ADR/ADS 本币金额统一显示真实单位，不再误写成美元现金分红；未知结构性行动默认待核实。
- 修复新增参考价文件导致 GitHub Actions 缓存版本变化、去重状态未恢复的问题：`state.json` 改用独立稳定缓存，并兼容迁移新旧两版历史缓存，避免再次把历史事件整批重放。

## 2026-09-03 · 新增 17 个合约标的公司行动监控

- 合约范围新增 **ORCL / AMZN / COIN / PLTR / IBM / HOOD / WDC / BE / ARM / TSM / RKLB / TQQQ / AXTI / MVLL / DELL / BABA / GLW**；现货维持 62 支，合约 22 → 39 支，去重后覆盖 73 → 81 个资产、可监控证券 67 → 75 支。
- 原本已在现货范围的 9 支现在标记为「现货+合约」；IBM / ARM / TSM / TQQQ / MVLL / DELL / BABA / GLW 以「仅合约」进入公司行动抓取。
- TQQQ 与 MVLL 按各自 ETF 本体监控；MVLL 不映射到 MRVL。Nasdaq 和 StockAnalysis 自动选择 ETF 路径；补入 GLW / IBM / TQQQ / TSM 官方分红入口，并同步补齐 NYSE 标的的 FINX RIC、Bot 说明、范围文档及发布回归断言。

## 2026-09-02 · 强制同步 Bot「最近更新」

- 新增仓库级维护规则：任何修复、规则/配置/数据修订或用户可见变更，都必须在同一任务内同步更新 `CHANGELOG.md`，否则不得宣告完成。
- 明确 Bot 更新链路为 `CHANGELOG.md` → 构建 → Pages `data.json` → Bot；发布类任务须核验生产 `changelog[0]`，不能把「本地已写」误报成「生产已更新」。
- 构建产物 `site_data.json` 不作为手工维护源，避免日志与线上数据假同步。

## 2026-09-01 · 单源事件纳入临近核验推送

- 单源、未见宣告日的预测继续保持 `confirmed=false` 和金额门禁，但不再静默：进入 30 天窗口知会一次，14 天内每日推送一次。
- 定时 Lark 与交互 Bot 明确拆分「正式催办」和「单源核验」；单源一律标记「待核实、勿执行」，不触发正式催办 @ 或运营动作。
- 核验提醒与正式催办使用独立去重状态；单源当天补到官方宣告或第二源时，会合并成一条「单源已转正式」提醒，不会被静默或重复展示。
- 新增真实 `build` 两阶段回归：单源先进入 forecast+核验提醒，再补第二源转 pending+正式催办。

## 2026-08-31 · 修正本周分红、催办口径与美东业务日

- **HD** 按公司公告正式化：$2.33，2026-08-20 宣告，2026-09-03 除息/登记，2026-09-17 派发。
- **QCOM** 更正为官方 2026-07-17 宣告（非 07-09）：$0.92，2026-09-03 除息/登记，2026-09-24 派发。
- **GOOGL / TER / WDC / NVDA** 补入本次 SEC 或公司官方公告，已核验事件不再停留在 `official=false`。
- 「本周」改为美东业务日起未来 7 个自然日，按除息/生效日对正式事件去重，预测不计；「临近催办」明确为独立的 14/30 天窗口。
- 所有抓取、倒计时、网页、推送与 Bot 日期判断统一为 America/New_York，并新增无网络回归。

## 2026-08-19 · 确认 BBX 为 BlackBerry 输入别名

- 业务确认 RFQ 代码 **`BBX` 即 `BB`（BlackBerry）**；公开市场 canonical ticker 继续使用 `BB`。
- Pages 下发 `BBX → BB`，因此 Bot 的查询、观察与确认入口均会自动规范化到当前覆盖标的；范围计数保持现货 62 / 合约 22 / 覆盖 73 / 监控 67。
- 回归测试将完整别名表锁定为 `BBX/BRKB/BRK.B/QNTX`，防止后续展示面再次漂移。

## 2026-08-17 · 全链路范围一致性与临近预警守卫

- 合约范围将旧 `SKHYNIX` 更正为当前 RFQ 的 **`SKHY`**，其作为股票纳入公司行动监控；范围现为现货 **62**、合约 **22**、覆盖 **73**、监控 **67**。
- `official_event_overrides` 与 `forecast_watch` 均加当前监控范围门禁，历史 V/旧资产只留审计，不会重新进入 Pages、Lark 推送或 Bot。
- Pages 下发 canonical ticker alias；Bot 支持 `BRK-B`/长代码，并接受 `BRKB`、`BRK.B`、`QNTX`。`BBX` 仍不自动映射到 `BB`，等待业务确认。
- 修复 15–29 天静默期可能发送无明细推送；Bot 的临近催办、预测、日历和风险卡截断时均显示总数和网页入口。
- 新增无网络回归：RFQ 精确集合、SKHY 监控、范围门禁、Bot 代码识别与截断提示均由 CI 检查。

## 2026-08-13 · 现货范围与 RFQ 支持范围对齐

- 从活动现货范围移除 **AMAT / BX / EBAY / GLW / IBM / KLAC / MSFT / V / WMT**；现货 **71 → 62**，合约维持 22，覆盖资产 **82 → 73**，可监控证券 **75 → 66**。
- 已移除标的不再抓取、生成预警、出现在资产覆盖或发布给 Bot 的活动参考链接中；V 的官方化案例改为仅供本地回归测试使用。
- 历史确认、审计留痕、预测状态与历史变更记录均保留，不作为当前 RFQ 支持范围的一部分。

## 2026-08-04 · 修复定时任务延迟后静默跳过推送

- 定时调度改用 GitHub 原生 `timezone: America/New_York`，删除按 Runner 启动时钟判断的双 cron 门禁，GitHub 延迟排队后仍会执行抓取和推送。
- 生产 Lark webhook 缺失、网络错误、HTTP 错误或 Lark 业务错误现在会让 Action 失败；只有投递成功或合法静默后才保存去重状态，Pages 仍会尝试刷新。
- 新增定时配置和 Lark 投递安全回归测试，避免再出现“workflow 绿但 build 没跑”或未确认投递却吞掉状态。

## 2026-07-30 · 全链路核验：分红官方化、统一引用与发布防漂移

- **Visa（V）正式化**：已登记 Visa canonical 官方 IR 股息表；官方记录为 2026-07-28 宣告、2026-08-11 登记、2026-09-01 派发、$0.6700。V 不再作为 Alpaca 单源预测或运营催办的矛盾状态。
- 新增 `refs.json.official_event_overrides`：仅容纳人工逐项核验的官方事件，并继续参与零容忍冲突检测；官方链接、精确 filing、IR、SEC 公司备案与 StockAnalysis 第三方交叉核对统一由 `run.py` 下发。
- 网页、月历、定时 Lark 推送、Bot 查代码/今日/本周/日历/临近催办/新公告/预测，以及确认留痕全部改用统一引用契约；Nasdaq 不再作为唯一分红核对链接，第三方明确标为“可能滞后”。
- Actions 改为 ET 夏冬令时自动守门的每日 09:35 / 12:45 / 16:05 三次扫描；Pages 产物缺失改为失败而非静默部署。新增 `check_surface_consistency.py` 回归检查。
- 更新根 README、Bot 部署说明、操作清单、待办和运营文档；范围口径统一为现货 71、合约 22、覆盖 82、监控 75。

## 2026-07-29 · 单源预估改为「预测观察」，不再触发执行催办

- 新增 `观察 CODE YYYY-MM-DD [备注]` 指令与 `data/forecast_watch.json`；首个样例为 **V 2026-08-11**，标为 Alpaca 单源预测、等待 Visa 公司宣告或第二个独立源。
- 未见宣告日的单源事件不再进入待执行、每日催办或人工确认积压；网页、定时推送、查代码和月历均显示「预测观察·不执行」。
- 自动追踪三种变更：公司宣告/第二源确认即转正式催办，日期或金额变化推更新，预计日已过仍未证实推失效；稳定观察项不会每日刷屏。

## 2026-07-29 · 飞书预警补第三方交叉核对入口

- 分红预警现在同时展示权威入口（宣告 filing / 公司 IR / Nasdaq）和 **第三方数据**（StockAnalysis 分红历史）。
- 单源预警可直接在卡片内交叉查看，不必只依赖 Alpaca 或 Nasdaq 的单一展示。

## 2026-07-23 · 拆股归组优化:一次拆股 = 一条事件(锚定除权日)

- 问题:同一次拆股的「登记日」和「除权/生效日」差 7–8 天,不同源各报其一,5 天归组窗把它拆成**两条**(登记日+除权日)→ 下游可能把系数**双算**;运营也要确认两次。
- 改:
  - `SPLIT_GROUP_WINDOW_DAYS=15`(拆股专用更宽窗),让 record+ex 并成一条(分红仍 5 天,不受影响)。
  - 拆股**锚定到较晚的除权/生效日**(reconcile:split 取 max ex_date;分红/filing 仍取 min)。
  - `SPLIT_COMPARE_FIELDS` 去掉 ex_date、只认**比例** —— record vs ex 是口径差非错误,不再报假冲突;真错日期(差 >15 天)仍会归不进组而成空缺被发现。
- 效果:CRWD 4:1 → 一条 2026-07-02;KLAC 10:1 → 一条 2026-06-12;各源齐、无空缺无冲突。登记日的"幽灵事件"消失,下游只读到一条(除权日),不会双算。
- 你已按除权日确认的 ack 仍生效(锚定日期正是除权日);登记日那两条空缺下次重跑后自动消失。

## 2026-07-23 · 临近催办加「数据源 + 两个核对入口」

- 每条显示 **📡 数据源**(单源/N源 + 具体源名),一眼看清值的出处。
- 每条加 **🔗 核对:[公司filing/SEC] · [第三方数据]** —— 第一方(src_url 具体 filing → 公司 IR(refs)→ SEC EDGAR 备案)+ 第三方(stockanalysis 聚合页),方便交叉核对后再决定要不要「确认」放行。
- refs(IR 映射)从 data.json 读,`ack` 惰性导入(bot 侧可用),不 import 仓库根的 config。

## 2026-07-23 · 修:「临近催办」发指令无响应

- Bug:`upcoming_card` 里 `import config` 在 bot 运行时报 `ModuleNotFoundError`(bot 跑在 `bot/` 目录,`config.py` 在仓库根、不在其 import 路径)—— 被 on_message 的 try 吞掉,发指令静默无回应(帮助却正常,因为不 import config)。
- 修:把催办文案 `alert_copy` **内联进 cards.py**(`_alert_copy`),cards 不再依赖 config。

## 2026-07-23 · 新增指令「临近催办」(可主动拉,不用等推送)

- 群里 @机器人 发 **临近催办 / 催办 / 临近 / 待执行** → 弹出已公告未发生的公司行动,按距除息天数排 + 催办文案(与推送同口径)。
- 指令 13 → **14**,四处一致检查通过。config 惰性导入(纯数据、CI 安全)。

## 2026-07-23 · 再移除 6 个现货(CAT/DELL/COST/TXN/LLY/SPCX)

- 现货 77 → **71**;合约仍 **22**;监控 80 → **75**。
- CAT/DELL/COST/TXN/LLY:本期不上,整体移出监控。
- **SPCX 仅从现货移除,合约保留** —— 合约固定 22 不变;SPCX 产品标签由 [现货+合约] 变为 [合约],仍经合约被监控。
- STRC 早已 delist(不在列表);SPCX1 无此代码。
- 副作用:CAT/DELL 的补确认、LLY 的单源推测随之作废,确认积压进一步清空。

## 2026-07-23 · 预警节奏改版:合并「临近预警+待执行」+ 30天知会/14天每天

- 按你的要求把两个清单合并成一个 **🔔 临近催办**;节奏改为:**进入 30 天窗口提醒一次 → 15–29 天安静 → ≤14 天每天提醒一次**(每天去重,流水线一天跑 3 次只推 1 次)。
- 文案随天数升级(14天窗口跟进 → 7天准备文案 → 3天收尾 → 1天最后确认)。
- `config`:新增 `ALERT_HEADSUP_DAY=30 / ALERT_DAILY_WITHIN=14 / alert_copy(days)`;`ALERT_ROUNDS` 弃用保留。
- `run.py`:`fired_rounds` 状态从"已触发轮次列表"改为 `{headsup, last_daily}` 字典(兼容旧数据)。
- 推送(notify_lark)与网页(report)都把「待执行」并入「临近催办」,不再单列;文本 digest 同步。
- 注:首次重跑因状态结构切换,可能对在窗口内的事件补推一次(一次性,之后正常)。

## 2026-07-23 · 本期不上 ADR,移出监控(现货 85→77)

- 移除 8 个真 ADR:**ARM / ASML / BABA / NOK / NVO / PAYP / SONY / TSM**(现货 85→77,监控 88→80,合约仍 22)。
- 保留 BB/BSP/NBIS/IREN —— 海外公司但直接上普通股(非 ADR)。
- `ADR_WHT`(母国+预扣税率)表**保留不删**,下期要上再把代码加回 SPOT_TICKERS 即可复用。
- 副作用:下次流水线重跑后,这些 ADR 的分红/冲突/空缺会自动从网站与推送里消失;**之前挂着的 ADR 待确认(ASML 7/28、TSM×3、NVO、SONY、NOK)也随之作废,不用再确认了**。

## 2026-07-23 · 掉线告警:bot 心跳(dead-man's-switch)

- 起因:Railway 试用到期把交互 bot 停了、好几天没人发现(网站/推送在 GitHub Actions 上没受影响,但确认/查代码等指令用不了)。
- 加 `bot._heartbeat_loop`:每 5 分钟 ping 一次 `HEARTBEAT_URL`(healthchecks.io 之类)。bot 挂了就不再 ping,监控方超时发邮件/告警。未配置该环境变量则静默跳过。
- 配套:Railway 用量邮件提醒设为 $10(异常涨用量早知道),硬上限保持 $150 不误停。

## 2026-07-16 · 修正海外标的分类 + 补 PAYP

- **PAYP = PayPay(日本 ADS)**:2026-03 Nasdaq 上市、6-K 外国发行人 —— 是真 ADR,补进预扣税表(日本 15.3%)。
- **修 BSP 分类**:之前误记「巴西 0%」,实为 **Bending Spoons·意大利**(2026-07 IPO),股息预扣 26%,已改;并补 NBIS(荷兰)、IREN(澳洲)。
- 该表现覆盖「真 ADR + 直接上市的海外普通股」:真 ADR = ARM/ASML/BABA/NOK/NVO/PAYP/SONY/TSM;海外直接上市 = BB/BSP/NBIS/IREN。成长股/不派息的先纳表,防将来派息漏掉预扣税判定。

## 2026-07-16 · ADR 预扣税保险:强制认税前毛额 + 提示防呆

- **要求**:分红一律认**毛额(税前)**。ADR 母国派息时会预扣税,存托行转的是**净额**,很多源(Alpaca/S&P/聚合页)报的就是净额。
- **表**:`config.ADR_WHT` 登记各 ADR 母国+预扣税率(TSM台21% / ASML荷15% / NVO丹27% / SONY日15.3% / NOK芬35% / BABA开曼0% 等)。
- **识别**:`reconcile.adr_tax()` —— 预扣税只会让金额变小,故**毛额=非净额源的多数票**,明显低于它(>5%)的源判为**净额**。实测 ASML 净/毛=0.85 恰好 15%(荷兰条约税率),铁证。
- **提示(三处)**:风险卡 / 网页 / 推送的 ADR 分红冲突下,红字标「⚠️ADR·母国预扣税:确认请用**毛额 X**(税前);Y 是净额(≈已扣N%),勿据此确认」。
- **确认防呆**:群里确认 ADR 分红时,若填的值像净额(低于毛额>5%),机器人回卡**红字警告**并提示毛额(仍记录,可覆盖重发)。
- 美股(HPE/KLAC/WMT)无外国预扣税、BABA 开曼 0%,均不误报。
- SONY/NOK 的**本币毛额**(JPY/EUR per share)从 6-K 精确核出留作 TODO;当前用源侧毛额(FMP/Tiingo),已能正确区分净额。

## 2026-07-16 · 冲突核对给两个源(权威+聚合)+ IR 改从 data.json 读

- **要解决冲突时给两个链接**:先最权威(T1 公司IR / T2 具体filing),再附**聚合页快速核对**。尤其 ADR:权威源是本币公告(TSM NT$6.0、NVO DKK7.95),聚合页补 USD,两边交叉核对。
- **IR 映射改从 data.json 的 `refs` 读**(不再依赖机器人本地能否读到 refs.json)——修复 NVO 明明配了 IR 却还显示 T3 聚合页的问题(机器人 Railway 部署读不到根目录 refs.json)。
- NOK 本轮是**空缺**不是冲突(FMP/Tiingo 源可用性波动导致 conflict/gap 来回切),所以不在风险卡的冲突区,在当日总览的空缺区。

## 2026-07-16 · 人工核实的 filing 直达(filing_overrides)—— 回答"为什么又成聚合页"

- 我在对话里逐封打开核对过的宣告 filing,以前没进系统,所以风险卡回退到 T3 聚合页。现登记进 `refs.json.filing_overrides`(key=代码|除息日),作为 **T2 SEC原文**直接用。
- 本批已核实并登记(都亲自打开确认含该金额):
  - HPE 06-16 → hpe-20260601.htm("$0.1425 common per share, record June 16")
  - HPE 03-24 → hpe-20260309.htm("$0.1425 … record March 24")
  - TSM 06-11 → …294/tsm-dividendadjustment(NT$6.0/股,Q4'25)
  - TSM 03-17 → …027/tsm-dividendadjustment(NT$6.0/股,Q3'25)
  - BABA 06-11 → …060224/…ex99-1(FY26 年报公告:"US$1.05 per ADS … June 11")
- NOK/SONY 暂未登记(分期派息结构复杂),仍走 T3;后续核实后补进 filing_overrides 或 IR。
- 取值仍只认人工登记,**不再 EFTS 全文猜**。

## 2026-07-16 · 核对来源改为置信度分级 + 关停乱猜 filing

- **关停 EFTS 猜 filing**:全文搜「dividend」会命中公司章程、发债 8-K、港交所月报等任何提到该词的文件(BABA/NVO 就被匹配错)。分红/拆股不再自动给"具体 filing";并购/退市仍用 SEC 源的真实 url(可靠)。
- **核对来源置信度分级**(`ack.verify_link`,风险卡链接标出档位):**T1 公司IR·最权威 → T2 SEC原文 → T3 聚合页(第三方·快速核对)**,1 没有看 2、2 没有看 3。
- **ADR 口径坑**:认**毛额 gross**(公司宣告税前)。但 ADR 的 USD 是**净额**——聚合页/S&P/Alpaca 显示的是扣预扣税后净额(如 TSM 显示 $0.742,非 gross $0.955)。**ADR 的毛额只在公司 IR 的本币公告里**(NVO DKK7.95/股、TSM NT$6.0/股)。故 ADR 要在 refs.json 填 IR(T1),别靠 T3。
- 已核实并加入 NVO 的 IR 分红页(直接显示 DKK 7.95/股)。
- `sec_filing.py` 保留但不再调用(留待将来有可靠定位方案再启用)。

## 2026-07-15 · 确认同时消解「空缺」(修:值确认了空缺还挂着)

- Bug:`run.py` 只把已确认事件从**冲突**里剔除,**空缺**没剔 —— 导致 NOK/SONY 明明值确认过了,却还以"yfinance 缺失"挂在空缺里。
- 修:确认(`acknowledged.json` 命中标的+日期)时,冲突和空缺**一起**剔除并记入 resolved,与机器人 `apply_acks` 口径一致。NOK 04-28/02-03、SONY 03-30 下次重跑自动消。
- 拆股空缺仍是"同一次拆股被记了 record/ex 两个日期"的归组产物(CRWD 4:1 record06-25·ex07-02;KLAC 10:1 record06-04·ex06-12),按除息日各确认即可。

## 2026-07-15 · 冲突核对链接直达具体 filing(不再给纵览)

- 反馈:风险卡冲突下的「权威核对」点开是 SEC 公司**全部备案列表**,不是那封具体公告;且「看数值」是第三方聚合页。
- 改:新增 `sec_filing.py` —— 用 EDGAR 全文检索(EFTS)按 CIK+关键词+表单(8-K 普通股 / 6-K 外国发行人)+除息日前时间窗,**直达那封宣告 filing 原文**。流水线(run.py)每次 build 顺带解析,存 `src_url` 进 data.json;实测 NVO/HPE/KLAC/NOK/SONY/BABA/TSM 都能定位到具体文档。
- 风险卡链接:有 `src_url` → **[SEC 原文(具体 filing)]**;定位不到 → 回退 [公司IR/SEC备案]。**去掉第三方聚合页链接**。
- best-effort:解析全程 try/except、每次调用 8–12s 超时、`SEC_FILING=0` 可一键关闭,绝不影响核对/报警主流程。
- 直达链接在**下次流水线重跑后**生效(它在 build 时解析并写入 data.json)。
- 修:`cards.py` 顶层别 import ack(ack 依赖 requests,而 CI「指令一致性检查」在装依赖前就 import cards,会炸)——改成兜底分支惰性导入。

## 2026-07-15 · 风险卡每条冲突直接带权威核对链接

- 『风险』是运营唯一能主动呼出、统一看所有冲突的地方 —— 现在**每条数据冲突下面都挂一个可点开核对的权威来源**:公司 IR → SEC EDGAR(8-K 普通股 / 6-K 外国发行人 ADR 都覆盖);分红另附 stockanalysis 聚合页快速看数值。
- 冲突行也标出关键日类型(除息日/生效日),与确认/留痕口径一致。
- 复用 `ack.authoritative_source()` / `quick_look()`,不用 Nasdaq(常空白、不覆盖 NYSE/ADR)。

## 2026-07-15 · 确认即时生效(不必等流水线重跑)

- **痛点**:卡片读的是 Pages 上的 data.json(每交易日只重算 3 次),所以刚『确认』完、@bot 看『风险』时那条冲突还在,看着像没生效。
- **修**:bot 每次取数后叠加实时的 `acknowledged.json`(`apply_acks`)—— 已确认的**字段冲突/数据空缺当场从风险、当日总览里剔除**,计数同步减少。你一发『确认』,下一次 @bot 就看不到那条了。
- 真实公司行动(拆股/并购日历项)不因确认而消失,只解除其金额门禁显示;冲突/空缺这类**数据报警**才会被确认清掉。
- 无 GH_TOKEN 时自动跳过(行为不变)。

## 2026-07-15 · 确认体验修正(日期标注 · 核对来源换更稳的源)

- **关键日标注**:确认卡片/留痕/Excel 现在明确写『**除息日**(分红)/ **生效日**(拆股)』,不再是含糊的「事件日」。异常里带的那个日期 = 除息日/生效日(不是登记日/派发日)。
- **核对来源不再用 Nasdaq 分红页**:它 JS 渲染、常空白,还不覆盖 NYSE/ADR(HPE、BABA 都点不出)。改为 **公司 IR(refs)→ SEC EDGAR 该标的全部备案**(8-K 普通股 / 6-K 外国发行人都能覆盖,始终有内容)。另加 `quick_look()` 给聚合页(stockanalysis)供人肉 eyeball。
- 已核实 HPE `0.1425`(2026-03-24 / 06-16,公司 8-K)、BABA `1.05`/ADS(2026-06-11,6-K)—— A 组这两条无误。

## 2026-07-15 · 确认留痕库(只追加审计底账)+ 「留痕」指令 + Excel 导出

- **落库**:新增 `data/ack_log.json` —— **只追加、永不删**的确认审计日志。每次『确认』记一条:确认时间(北京/UTC)、确认人(显示名+open_id)、标的/事件/事件日、确认值、**改值前的旧值**、自动带的**核对来源**、备注。
  - `data/acknowledged.json` 仍是「当前生效值」(同标的+同日去重,pipeline 读它停报警);两者分离 —— 生效值会被覆盖,但留痕库保留每一次改动。
- **核对来源自动带**:`ack.authoritative_source()` 按标的+类型给最权威链接(并购/退市→SEC;分红→公司IR/Nasdaq;拆股→IR/SEC 8-K),确认人点开即可核对。
- **备注**:`确认 代码 [值] [日期] [备注]`,末尾自由文字记录你核对了什么(如 `确认 KLAC 2.3 2026-05-18 已比对公司8-K`)。
- **确认人姓名**:bot 用通讯录 API 把 open_id 解析成显示名(无权限则回退 open_id)。
- **新指令「留痕 / 审计」**:群里 @机器人 发『留痕』拉最近确认记录(可加代码只看某标的)。经 GH API 读日志,**不落网页**。
- **Excel 导出**:`tools/export_ack_log.py`(随时可跑,拉线上日志导 xlsx;改值行标黄、来源列可点)。
- 指令 12 → **13**,四处一致检查通过。

## 2026-07-14 · 「确认」支持指定日期(修一个会确认错的真 bug)

- **Bug**:`确认 KLAC 2.3` 不带日期时会匹配该标的的**任意**事件。而 KLAC 有两条冲突、正确值不同(05-18 是 `2.3`、02-17 是 `1.9`),ASML 同理(`3.16845` / `1.9064`)—— 一次确认会把错的值盖到另一条上。
- **修**:`确认 代码 [正确值] [日期]`。先摘 `YYYY-MM-DD`(否则 `2026` 会被当成金额),再取数值;按日期在 conflicts/pending/calendar/gaps 里定位事件类型。不带日期时行为不变(取该标的第一条冲突)。
- 同一标的有多条**值不同**的异常时,**必须带日期**,如 `确认 KLAC 2.3 2026-05-18`。
- 四处同步:`COMMANDS` desc / README 指令清单 / `confirm_card` 用法文案 / 关于卡片。

## 2026-07-14 · 全量文档/展示面同步(关于卡片 · README · 官网 · 检查清单)
- **关于卡片**重写:8 源、关键日链(宣告·登记·除息/生效·派发)、**取值多数票 + 各源口径坑**、**🚦金额门禁**、**🙋人工介入闭环**(已挂 N 天 / 超期@ / 确认是唯一出口)
- **「确认」指令描述**改为「人工放行异常」——它现在不只是消解冲突,还能解除单源门禁,且对所有事件生效
- **README** 重写开头 + 新增「取值规则与金额门禁」「人工介入闭环」两节;数据源改 8 源(FINX 已上线);指令清单同步
- **官网**新增常驻「📖 规则说明」折叠区:解释为什么有些金额不给数字、怎么解除
- **UPDATE_CHECKLIST** 加两条血泪教训:①取值只能有一个真相(`reconcile.pick_value`);②**日历是容易漏的第四条展示路径**,有独立取值代码
- **TODO.md** 列出待人工确认的存量异常(ADR扣税 / 拆股回溯 / 四舍五入 / 单源 / 未宣告预估)

## 2026-07-14 · 门禁补漏:日历也纳入 + 单源金额同样封锁
- **发现日历视图是另一套取值代码**(`run.py._ck` / `report._pick` 都是「第一个源赢」),**完全绕过了多数票、确认覆盖和门禁** —— 上一版只堵了「待执行/催办/新公告」
- 取值逻辑收敛为唯一真相 `reconcile.pick_value()`,run.py 与 report.py 全部改走它;日历格子也上门禁(冲突→`⚠️待确认`,单源→`⚠️单源`)
- **单源金额同样封锁**:只有 1 个源报的金额/比例 = 没交叉验证过,不能当权威值。三面统一显示
  **「⚠️单源未交叉验证(x)· 待人工确认,勿据此执行」**
  - 实例:TSM 9/16 只有 Alpaca 一个源(报的还是 ADR 扣税净额),多数票无从投起 —— 现在直接封锁而不是印一个数
  - 实例:CRWD 4:1、KLAC 10:1 的拆股比例也是单源 → 同样需人工确认
- 人工确认后 `g.acked=True`,门禁解除、按确认值显示(日历/详情列/卡片全生效)
- **人工确认改为对「所有事件组」生效**(原来只作用于冲突):单源事件也能被人工放行,否则确认了门禁还封着
- 实测:KLAC 10:1 单源拆股 → 封锁 → 发「确认 KLAC」→ 放行显示 `10:1`

## 2026-07-14 · 金额门禁:未确认冲突的事件不给「确定值」
- 之前即使各源对不上,卡片/网页照样印一个金额,运营可能直接照着执行 —— 这是真正会绕过人工的地方
- 现在:只要该事件还有**未确认的冲突**,三个面一律不显示确定金额,改为
  **「⚠️各源不一致(0.955 / 0.93933)· 待人工确认,勿据此执行」**
- 人工发 `确认 代码 值` 后 → 冲突消解、门禁解除、按确认值正常显示(实测 TSM:封锁 → 确认 0.955 → 恢复 `$0.955`)
- 注意区分:**冲突判定(reconcile.py)从未改动**,零容忍照旧,一条不豁免;改的只是「人还没确认之前显示什么」

## 2026-07-14 · STRC 下架(本轮不上)
- 现货去掉 STRC(Strategy 优先股):86 → **85**,监控 89 → **88**;合约仍 22 不变
- 它的 2 条待执行 + 相关冲突随之消失

## 2026-07-14 · 人工介入闭环:异常挂天数 + 超期 @ 升级(零容忍不豁免)
- **明确不做口径豁免**:ADR 扣税 / 拆股回溯 / 四舍五入造成的差异照报,每条都必须人工看过
- 每条异常(字段冲突 / 数据空缺 / 待执行里「未见宣告日的单源预估」)记录**首次出现日**,算出挂了几天
- **每次扫描都重报**,不确认就一直挂;推送与官网都显示「已挂 N 天」,超 `REVIEW_ESCALATE_DAYS`(默认 3 天)标红
- **超期自动 @ 负责人**:推送顶部「🙋 待人工确认 N 条 … 有 M 条超过 3 天没人确认」+ @ refs.json 里的 open_id
- **唯一消解出口**:群里发 `确认 代码 [正确值]` → 从队列移除并记入「已确认」(留痕:谁确认、何时、以什么值为准)
- 官网预警面板顶部加醒目横幅;digest 标题改「需人工确认」

## 2026-07-14 · 数据正确性:金额取多数票 + 区分「真已宣告 / 单源预估」
- **金额取值改多数票 + 源优先级**(原来是「第一个源赢」,顺序不代表谁对)。三个真错被修:
  - yfinance 会**按拆股回溯调整**历史分红:KLAC 10:1 后把 2.3 报成 0.23 → 现取 2.3(3 源一致)
  - yfinance **四舍五入**到 3 位:WMT 0.2475 被报成 0.248 → 现取 0.2475
  - Alpaca 对 **ADR 报的是扣预扣税后的净额**:ASML=gross×0.85(荷兰15%)、TSM×0.79(台湾21%)
    → TSM 站上曾显示 0.879761(净额),现纠正为宣告原值 **1.11362**
  - 新增 `config.SRC_PRIORITY`(Nasdaq/FINX/FMP 优先于 yfinance/Alpaca),仅平票时启用
- **「待执行」区分真伪**:无宣告日且单源 = Alpaca 的**预估**,公司并未实际公告。经查 LLY 8/14、CAT 7/20、DELL 7/21、GLW 8/31 Nasdaq 全无记录 —— 现标 `confirmed=false`,三面均显示「⚠️ 未见宣告日,可能是预估,请勿据此执行」
- 教训:多源不是「谁先谁算」,得看谁**报的是公司宣告的原值**

## 2026-07-14 · 现货扩容 24 → 86(新上 62 支)
- 现货新增 62 支(AAOI/ADBE/ALAB/AMAT/ARM/ASML/BABA/BRK-B/BX/CAT/COST/CRM/CSCO/DELL/DIS/HD/IBM/JPM/NFLX/ORCL/QCOM/SONY/TSM/V/WMT 等);监控标的 27 → **89**
- **合约维持 22 不变**;SPCX 虽不在新清单但已上线,保留
- **BRK-B 格式坑**:Berkshire B 类必须写 `BRK-B`(SEC/yfinance/Tiingo/FMP 都用这个)。原写法 `BRK.B` 会导致 5/7 源不可用、0 条数据 —— 已修
- **CBRS 修正归类**:原误标 `commodity`(不监控),实为 Cerebras(AI 芯片,股票)→ 改回 equity,现已纳入监控
- 新增开关 `BASELINE_NEW_TICKERS`:新标的首次纳入时历史事件是否静默建基线。默认 `False`(照常推「新发现」);置 `True` 则不刷屏。本次上新实测:False→72 条新发现,True→0 条
- 抓取耗时 89 支约 3–4 分钟(原 1 分钟);Alpha Vantage 仍限前 24 支

## 2026-07-14 · 一键触发 Action(tools/trigger.sh)
- 新增 `tools/trigger.sh`:一行触发 Action + 等跑完 + 自动核验网页刷新时间,不用再开网页点按钮
- 用 gh CLI 的登录态(workflow_dispatch 需 Actions:write,GH_TOKEN 那个 Contents-only 的 PAT 不够)
- 一次性准备:`brew install gh && gh auth login`;之后 `./tools/trigger.sh`(加 `-n` 只触发不等)

## 2026-07-14 · 关键日补上「宣告日」(三面统一)
- 催办/待执行原来只显示 除息·登记·派发,漏了宣告日(数据里一直有 `decl`,只是没渲染)
- 统一为 **宣告 · 登记 · 除息/生效 · 派发**(与「查代码」的关键日链一致);缺哪个就不显示哪个
- 三个展示面同步:推送bot(`notify_lark._dates`)、交互bot(`cards._dates`)、官网+digest(`report.py`)
- 拆股类事件标「生效」而非「除息」;顺带修掉官网「首发」HTML 被 escape 成文本的小 bug

## 2026-06-22 · FINX 拆股比例归一(1-250→1:250)
- 实查 GDC 反向拆股时发现:FINX 用 `1-250` 表示比例,其它源用 `1:250`,被零容忍误判成冲突
- `sources._finx_ratio` 统一成 `num:den`;GDC 拆股恢复为「已确认」(四源一致)

## 2026-06-22 · FINX 空缺降噪 + 新增 TODO.md
- 历史覆盖短的源(FINX)只对近 45 天内及未来事件参与「空缺」判定:老历史它没有不再误报(META/GOOGL/NVDA 三条历史空缺消除)
- 配置项 `config.SHORT_HISTORY_SOURCES` / `SHORT_HISTORY_GAP_DAYS`;逻辑在 `reconcile.py`
- 新增 `TODO.md`(内部技术待办):记 FINX 接口稳定后复核 RIC/历史空缺/字段、轮换密码等;README 可维护文件索引补上它

## 2026-06-22 · 新增第 8 源 FINX(TRKD-HS)
- 接入 FINX 静态数据 API:JWT 认证(`/auth/token` 换 token,其余带 `x-auth-token`),抓分红/拆股/并购
- 用 RIC 寻址(如 `TSLA.O`),映射表 `config.FINX_RIC`(默认 `.O`,可按实际上市所调)
- 凭证只走环境变量 `FINX_USER`/`FINX_PASS`(`FINX_BASE` 可切 UAT);**未配置即静默跳过,不影响其它源**
- 注:供方接口仍在调整(约 2 周、demo 阶段),字段做了防御式解析;稳定后再核对 RIC/字段
- Actions 工作流传入 `FINX_USER/FINX_PASS/FINX_BASE` secret;网页源健康矩阵加 FINX 列(否则 FINX 抓了也不显示/不生效)

## 2026-06-21 · README 查漏补缺
- 标的数订正:监控 24 → 27(现货 24 个股 + QQQ/EWY/DRAM 三个 ETF)
- 补充 GH_TOKEN 说明 + 可维护文件索引(refs.json / CHANGELOG / UPDATE_CHECKLIST / requests.md)

## 2026-06-21 · 新增「需求提报」指令
- 群里 @机器人 + `需求 你的想法` → 追加到仓库 requests.md(带提报人 open_id + 时间),供负责人汇总迭代
- 复用 GH_TOKEN;指令数 11 → 12

## 2026-06-21 · 填入 @ 名单(两个 open_id)
- 从 Railway 日志读到两位的 open_id,填入 refs.json 的 alert_mention_open_ids
- 已实测:清缓存重跑触发 AVGO 催办,Lark 推送成功并 @ 到两人

## 2026-06-21 · 轮询预警 @ 提醒
- 推送「临近预警(催办)」时,在催办区顶部 @ 指定的人
- @ 名单维护在 refs.json 的 alert_mention_open_ids(填 open_id;`all`=@所有人;留空=不@)
- 仅当有催办事项且配置了名单才 @,避免无谓打扰

## 2026-06-21 · 最近更新按钮直达更新日志 tab
- 网页支持 `?tab=log` / `#log` 打开即切到对应标签页
- 机器人「最近更新」的「查看完整更新日志」按钮指向 `?tab=log`,一点直达,不再落在日历页

## 2026-06-21 · 更新日志独立 tab + 日期按 git 修正
- 官网新增「🆕 更新日志」独立标签页(原来塞在预警面板里),含更新日志 + 参考链接维护台
- CHANGELOG 日期按 git 提交时间订正(此前多条误写成 06-19,实为 06-20/06-21);补回漏记的「IR 维护台填入」条

## 2026-06-21 · bot 打印发送人 open_id(便于维护 @ 联系人)
- on_message 增 `[sender] open_id=` 日志:让对方 @ 一次机器人,从 Railway 日志取其 open_id,用于后续 @ 联系人表

## 2026-06-21 · 官网补齐第三面(核对链接 + 已确认)
- 官网预警面板新增「待执行」列表,分红条目带核对链接(8-K/IR/Nasdaq),与机器人一致
- 新增「✅ 已人工确认(finalize)」板块,显示已确认冲突(以确认值为准)
- 三个展示面(交互bot/推送bot/官网)就此对齐

## 2026-06-21 · 关于补充近期能力 + 更新检查清单
- 关于卡片正文补上「核实与确认」:每条带核对链接(SEC/8-K/IR/Nasdaq)、冲突可发『确认 代码 值』finalize
- 新增 UPDATE_CHECKLIST.md:每次改完按它过一遍(同步四处 + CHANGELOG + 关于、真 import 验证、推后触发 Action)

## 2026-06-21 · 修复 Action 崩溃 + 8-K 改用 Item 8.01 判定
- 修 run.py 漏 import re/requests 导致每次 Action 失败(NameError)
- 分红 8-K 匹配改用元数据:仅认 Item 8.01(宣告分红标准载体)、±3 天最近;不再抓正文
- 投票结果(Item 5.07)等被正确排除,根治 META 误挂;宁少挂不挂错,落空回退 IR/Nasdaq

## 2026-06-21 · IR 维护台填入 AAPL/MSFT
- 逐个核实 IR 分红页:AAPL/MSFT 可用已填;NVDA(重定向)/AVGO(404)/MU/INTC/LLY(JS)/GOOGL/META(无专门页)→ 回退 Nasdaq
- 不塞死链;留空待日后补可信 URL

## 2026-06-21 · 参考链接维护台 + 分红 IR 链接
- 新增 refs.json(单一维护源):每标的官方 IR 分红页 URL,改这一个文件即可
- 分红核对链接优先级:宣告 8-K(自动)→ IR 分红页(refs.json)→ Nasdaq(回退)
- 网页新增「🔗 参考链接维护台」板块,显示各标的 IR 链接 / 未维护状态

## 2026-06-21 · 分红匹配宣告 8-K
- 按分红宣告日匹配该标的的宣告 8-K,分红也能链到那份具体 SEC 文件
- 匹配不到才回退 Nasdaq 分红记录;reconcile 存 8-K 的 form/items 供匹配

## 2026-06-21 · 核对链接指向具体文件
- filing 类事件:链接直接指向该 filing 的 SEC 原文文件(不再是整列表)
- 分红:指向该标的 Nasdaq 分红记录;去掉「全部 SEC 备案」大列表

## 2026-06-21 · 查代码加用法说明
- 只发『查代码』(不带代码)时,回一段用法:@CA问答助手 + 代码(如 AVGO)即可弹出该标的信息
- 帮助里同步可见

## 2026-06-21 · 人工确认(finalize 冲突)+ 推送卡片核对链接
- 新增「确认」指令:群里 `确认 META 0.525` → 写入 data/acknowledged.json(GitHub API,需 GH_TOKEN)
- run.py 读确认:把已确认的冲突从报警剔除(停推 + 网页不再红)、用确认值覆盖金额、记入 resolved
- 「待执行」「字段冲突」每条附核对链接,方便人工 double-check
- 细粒度 PAT 仅需本仓库 Contents 读写;配在 Railway

## 2026-06-20 · 新增「查代码」单标的查询
- @我 + 空格 + 代码(如 `查 AVGO` 或直接 `AVGO`)→ 该标的全量公司行动
- 分红/拆股列出宣告/登记/除息/派发四个关键日 + 各自距今天数
- 重大事件(并购/退市/分拆/要约)附 SEC 原文链接
- 每条带现货/合约风控动作 + 按 30/14/7/3/1 天的运营公告处理提醒

## 2026-06-20 · 指令精简:今日窗口/新公告改版/删预警
- 今日:改为 T0 前后 24 小时窗口(昨/今/明的关键日),今天的条目标「🔴 今天」
- 新公告:列最近 5 个宣告(declaration)的事件,已派发完的标「✅ 已结束」
- 删除「预警」指令(与「风险」重复;数据质量冲突/空缺已在风险里)

## 2026-06-20 · 时间戳带时区标注
- 生成时间改为「美东 ET + 北京」双标注(原来是服务器 UTC 裸时间,易误解)
- 定时仍按美东盘中 9:35/12:45/16:05 ET;GitHub 定时为尽力而为,可能延迟

## 2026-06-20 · 风控显示日期 + DRAM 归类 ETF
- 风控清单每条显示日期:filing 标「申报 日期」(注明是申报日,非执行日),拆股标「生效 日期」
- DRAM 由「商品(不监控)」改为「ETF(监控分红)」

## 2026-06-19 · 指令顺序按用户动线调整
- 指令重排:关于 → 帮助 → 最近更新(上手/元信息),再按紧迫度高→低
- 顺序同时是匹配优先级:覆盖(关键词较泛)放最后;帮助去掉 `?` 避免「…?」误判
- README 指令清单同步重排,一致性检查通过

## 2026-06-19 · SEC 细分 + 更新日志 + 指令一致性
- SEC 原文按 8-K Item 显示具体事件,标注是否「公司行动相关」,并显示申报时刻;支持翻页(30 条/页)
- 新增 `最近更新` 指令 + 本更新日志;指令一致性检查接入 CI(改指令必同步)
- state 改用缓存持久化,云端不再提交仓库(消除推送冲突)

## 2026-06-19 · 资产覆盖 + 三板块去重
- 资产覆盖:现货/合约分开,全部 35 个资产纳入;ETF 监控分红,商品/海外标「不适用」
- 报警去重:催办 > 新公告 > 待执行,同一事件只出现一次
- 机器人新增:关于 / 风险 / 今日 / 本周 / 新公告 / 覆盖
- 全站展示「首发(公告)日」;新公告(declaration date)即时推送;每交易日 3 次扫描

## 2026-06-18 · 交互机器人 + 月历
- CA问答助手上线(@ 指令);当月月历图(代码直绘,去掉网页截图)
- 修复:宣告日字段丢失、图片上传、仅在被 @ 时响应

## 2026-06-18 · 首次上线
- 多源(7)交叉核对、零容忍;网页面板 + 月历;Lark 定时推送;GitHub Actions + Pages 部署
